{
  "name": "simaqadeer-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sima Qadeer author site — Node/Express wrapper around the static build at camster91/simaqadeer.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/smoke.js",
    "deploy": "ssh hostinger 'bash -lc \"cd /root/simaqadeer-app && git pull --ff-only && /root/simaqadeer-app/scripts/deploy.sh\"'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
